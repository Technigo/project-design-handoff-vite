import { Nav } from './Nav'
import { Logo } from './Logo'
import { Button } from './Button'

export const Header = () => {
  return (
    <header>
      <Nav />
      <Logo />
      <Button />
    </header>
  )
}
