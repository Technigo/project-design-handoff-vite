import { Button } from "../reusables/Button";

// import styled from "styled-components";

export const Header = () => {
  return (
    <section>
      <Button>Check schedule</Button>
    </section>
  );
};
