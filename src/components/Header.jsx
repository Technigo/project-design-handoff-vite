import { Button } from "../reusables/Button";
import "../App.css";

export const Header = () => {
  return (
    <section>
      <h1>Header</h1>
      <Button>View schedule</Button>
    </section>
  );
};
